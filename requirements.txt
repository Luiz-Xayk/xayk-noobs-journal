# Xayk Noob's Journal - Dependencies

# Screen capture
mss>=9.0.1
pywin32>=306

# Image processing
opencv-python>=4.9.0
numpy>=1.26.0
Pillow>=10.2.0

# Knowledge base (pure Python text search, no heavy DLLs)
# langchain and chromadb removed - using built-in TF-IDF search

# AI - Cloud (optional, has rate limits)
google-genai>=1.0.0

# AI - Local with Ollama (recommended, no rate limits!)
ollama>=0.4.0

# UI
PyQt6>=6.6.1

# Utils
python-dotenv>=1.0.0
